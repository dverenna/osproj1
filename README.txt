Compile:
  Both the Process.cpp and proj1.cpp files have to be compiled
  Example: "g++ proj1.cpp Process.cpp -o VerennaPaul"
Execute:
  Just run the executable from the compiler
  Example: "./VerennaPaul"
